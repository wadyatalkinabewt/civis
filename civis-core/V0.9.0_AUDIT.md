# Trust Gating System v0.9.0 — Implementation Audit

**Purpose:** Hand this file to a fresh coding-agent session to verify the v0.9.0 implementation is correct. The auditor should read every file listed, check each requirement, and flag any discrepancies.

**Build plan:** `C:\Users\Galbraith\.agent\plans\stateful-wondering-prism.md`

---

## Audit Instructions

Read each file below. For each one, verify the stated requirement is implemented correctly. Mark PASS or FAIL. If FAIL, explain what's wrong.

---

## 1. Database Migration

**File:** `civis-core/supabase/migrations/010_trust_gating.sql`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 1 | Adds `trust_tier` column (text, NOT NULL, default 'standard', CHECK constraint on 'unverified'/'standard'/'established') | Read the ALTER TABLE statement |
| 2 | Adds `github_signals` column (jsonb, nullable) | Read the ALTER TABLE statement |
| 3 | Adds `card_fingerprint` column (text, nullable) | Read the ALTER TABLE statement |
| 4 | `check_passport_limit()` is REPLACED (CREATE OR REPLACE) with citation-based logic | Verify the function body counts inbound extension citations from OTHER developers, sets max to 5 if citations >= 1, else max 1 |
| 5 | Does NOT create a new trigger — reuses existing `trg_passport_limit` from migration 006 | Confirm there is NO `CREATE TRIGGER` statement in this file |
| 6 | RPC `get_developer_construct_count(p_developer_id uuid)` returns INT | Read the function |
| 7 | RPC `get_developer_inbound_citation_count(p_developer_id uuid)` counts DISTINCT source_agent_id, filters by extension type, non-rejected, different developer | Read the function |
| 8 | RPC `promote_trust_tier(p_developer_id uuid)` upgrades to 'established' only if not already established, only if citation count >= 1 | Read the function |
| 9 | RPC `check_card_fingerprint(p_fingerprint text, p_developer_id uuid)` returns boolean, checks for DIFFERENT developer | Read the function |

---

## 2. GitHub Signal Scoring

**File:** `civis-core/lib/github-signals.ts`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 10 | Pure function `computeGitHubSignals(ghUser)` exported | Check exports |
| 11 | 4 binary signals: account_age >= 30 days, public_repos >= 1, followers >= 1, has_bio (non-empty string) | Read signal computation |
| 12 | Pass threshold is 3 of 4 | Check `PASS_THRESHOLD` constant and `passed: score >= PASS_THRESHOLD` |
| 13 | Returns object with: account_age_days, public_repos, followers, has_bio, score, passed | Check return type and value |
| 14 | No side effects, no database calls, no imports beyond types | Confirm purity |

---

## 3. Auth Callback

**File:** `civis-core/app/feed/auth/callback/route.ts`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 15 | `MINIMUM_ACCOUNT_AGE_DAYS` constant is GONE | Search for it — should not exist |
| 16 | Hard age check (`accountAgeDays < MINIMUM_ACCOUNT_AGE_DAYS`) is GONE | Search — should not exist |
| 17 | Imports and calls `computeGitHubSignals(ghUser)` after blacklist check | Read the flow |
| 18 | Creates new developer with `trust_tier` and `github_signals` fields | Check the insert statement |
| 19 | If signals.passed → trust_tier = 'standard', redirect to /console | Check branch |
| 20 | If !signals.passed → trust_tier = 'unverified', redirect to /verify | Check branch |
| 21 | On signal failure: does NOT call `supabase.auth.signOut()` | Critical — search for signOut calls after signal check. There should be NONE |
| 22 | Returning user with existing record: queries `trust_tier` | Check the existing dev query selects trust_tier |
| 23 | Returning unverified user → redirected to /verify (not /login, not signed out) | Check the existingDev branch |
| 24 | Returning standard/established user → redirected to /console | Check the existingDev branch |

---

## 4. Verify Page

**Files:** `civis-core/app/feed/verify/page.tsx` + `civis-core/app/feed/verify/client.tsx`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 25 | Server component fetches developer record with trust_tier and github_signals | Read page.tsx |
| 26 | If trust_tier !== 'unverified', redirects to /console | Check redirect logic |
| 27 | If not authenticated, redirects to /login | Check user check |
| 28 | Client component displays 4 signal check results (pass/fail indicators) | Read client.tsx UI |
| 29 | Shows score "X/4 passed (3 required)" | Check score display |
| 30 | "VERIFY WITH $1" button calls POST /api/stripe/checkout | Check fetch call |
| 31 | On successful response, redirects to Stripe via window.location.href | Check redirect |
| 32 | Shows cancellation message when ?cancelled=true | Check searchParams |

---

## 5. Stripe Checkout Route

**File:** `civis-core/app/api/stripe/checkout/route.ts`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 33 | Requires authenticated Supabase session (returns 401 if not) | Check auth check |
| 34 | Creates Stripe Checkout Session with mode: 'payment' | Check session creation |
| 35 | Line item: 1x $1.00 USD, name "Civis Identity Verification" | Check line_items |
| 36 | metadata includes developer_id | Check metadata field |
| 37 | payment_intent_data.metadata includes developer_id | Check — this is needed to access payment method after |
| 38 | success_url points to /console?verified=true | Check URL |
| 39 | cancel_url points to /verify?cancelled=true | Check URL |
| 40 | Returns { url: session.url } | Check response |

---

## 6. Stripe Webhook

**File:** `civis-core/app/api/webhooks/stripe/route.ts`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 41 | Uses `request.text()` for raw body (NOT request.json()) | Critical — Stripe signature verification requires unparsed body |
| 42 | Verifies Stripe signature with STRIPE_WEBHOOK_SECRET | Check constructEvent call |
| 43 | Handles `checkout.session.completed` event | Check event.type |
| 44 | Extracts developer_id from session.metadata | Check metadata access |
| 45 | Idempotent: checks if trust_tier is still 'unverified' before processing | Check the developer query |
| 46 | Retrieves PaymentIntent → PaymentMethod → card.fingerprint | Check the chain of Stripe API calls |
| 47 | Calls RPC `check_card_fingerprint(fingerprint, developer_id)` | Check RPC call |
| 48 | If duplicate: calls `stripe.refunds.create()` and does NOT upgrade tier | Check duplicate branch |
| 49 | If unique: updates developer to trust_tier='standard', stores card_fingerprint | Check unique branch |
| 50 | Update includes defensive `eq('trust_tier', 'unverified')` to prevent race conditions | Check update query |
| 51 | Returns 200 with `{ received: true }` on all paths | Check all return statements |

---

## 7. Middleware

**File:** `civis-core/middleware.ts`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 52 | `/api/webhooks` paths are excluded from alpha gate | Check for isWebhook variable and its inclusion in the gate condition |
| 53 | The check is `url.pathname.startsWith("/api/webhooks")` | Verify exact string match |
| 54 | Gate condition is now: `!hasAccess && !isGatePage && !isGateApi && !isWebhook` | Read the if statement |

---

## 8. Console Access Control

**File:** `civis-core/app/feed/console/page.tsx`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 55 | Queries developer trust_tier after user auth check | Read the serviceClient query |
| 56 | If trust_tier === 'unverified', redirects to /verify | Check redirect |
| 57 | Fetches inbound citation count via RPC `get_developer_inbound_citation_count` | Check RPC call |
| 58 | Passes `inboundCitationCount` to ConsoleClient (both empty and full passport cases) | Check both return statements |

**File:** `civis-core/app/feed/console/client.tsx`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 59 | ConsoleClientProps includes `inboundCitationCount: number` | Check interface |
| 60 | Progressive access indicator shown when `inboundCitationCount === 0 && passports.length >= 1` | Check conditional render |
| 61 | "Mint Another Passport" button disabled when `passports.length >= (inboundCitationCount >= 1 ? 5 : 1)` | Check disabled prop |

---

## 9. Progressive Passport Minting

**File:** `civis-core/app/feed/console/actions.ts`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 62 | The old hard-coded `>= 5` check is GONE | Search — should not see `if ((count ?? 0) >= 5)` |
| 63 | Calls RPC `get_developer_inbound_citation_count` with user.id | Check RPC call |
| 64 | maxAllowed = (citationCount >= 1) ? 5 : 1 | Check logic |
| 65 | Error message: 'Earn citations from other developers to unlock additional passport slots.' | Check error string |

---

## 10. Auth Extension + Citation Gating

**File:** `civis-core/lib/auth.ts`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 66 | `authenticateAgent()` return type is `{ agentId: string; developerId: string } | null` | Check return type |
| 67 | Query joins agent_entities to get developer_id: `.select('agent_id, agent:agent_entities!inner(developer_id)')` | Check select statement |
| 68 | Returns both agentId and developerId | Check return statement |

**File:** `civis-core/app/api/v1/constructs/route.ts`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 69 | Citation gating: if citations.length > 0 AND constructCount === 0, return 403 | Check the new block after Zod validation |
| 70 | Uses `auth.developerId` (not agentId) for citation gating RPC call | Check the RPC parameter |
| 71 | Error message: 'Citations are unlocked after posting your first build log.' | Check error string |
| 72 | Auto-promote: calls `promote_trust_tier({ p_developer_id: auth.developerId })` after base rep increment | Check the new RPC call |

---

## 11. Login Page Cleanup

**File:** `civis-core/app/feed/login/page.tsx`

| # | Requirement | How to verify |
|---|-------------|---------------|
| 73 | `account_too_new` is NOT in ERROR_MESSAGES | Check the object keys |
| 74 | Remaining errors are: blacklisted, auth_failed, no_code, no_provider_token, github_api_failed | Check the object |

---

## 12. Config & Dependencies

| # | Requirement | How to verify |
|---|-------------|---------------|
| 75 | `stripe` is in package.json dependencies | Run `grep stripe civis-core/package.json` |
| 76 | TypeScript compiles with zero errors | Run `cd civis-core && npx tsc --noEmit` |
| 77 | Stripe API version matches installed SDK | Both routes use `2026-02-25.clover`, verify against `node_modules/stripe/types/apiVersion.d.ts` |

---

## 13. End-to-End Flow Verification

Trace these flows through the code and confirm each step happens:

### Flow A: New user passes signal score
```
OAuth callback → ghUser fetched → computeGitHubSignals() → score >= 3 →
insert developer (trust_tier='standard', github_signals=...) → redirect /console →
console page checks trust_tier → not 'unverified' → allows access
```

### Flow B: New user fails signal score → pays $1 with unique card
```
OAuth callback → ghUser fetched → computeGitHubSignals() → score < 3 →
insert developer (trust_tier='unverified') → redirect /verify (no signout) →
verify page shows signals + $1 button → user clicks → POST /api/stripe/checkout →
Stripe session created → user pays → webhook fires →
webhook: text() for body → verify signature → extract developer_id →
check trust_tier is 'unverified' → retrieve PaymentIntent → get card.fingerprint →
check_card_fingerprint() returns false → update trust_tier='standard', store fingerprint →
user returns to /console?verified=true → console allows access
```

### Flow C: Duplicate card attempt
```
Same as Flow B, but check_card_fingerprint() returns true →
stripe.refunds.create() called → trust_tier stays 'unverified' →
user stays on /verify
```

### Flow D: Returning unverified user
```
OAuth callback → existing developer found → trust_tier = 'unverified' →
redirect /verify (no signout, session alive) → user can pay $1
```

### Flow E: First build log with citations blocked
```
Agent posts to /v1/constructs with citations array →
authenticateAgent() returns { agentId, developerId } →
get_developer_construct_count(developerId) returns 0 →
citations.length > 0 → return 403 "Citations are unlocked after..."
```

### Flow F: Citation-based unlock
```
Agent posts first build log (no citations) → success →
Other developer's agent cites this log → citation inserted →
promote_trust_tier() called → developer becomes 'established' →
Agent tries to mint 2nd passport →
get_developer_inbound_citation_count returns >= 1 → maxAllowed = 5 → allowed
```

---

## 14. Version & Changelog

| # | Requirement | How to verify |
|---|-------------|---------------|
| 78 | CHANGELOG.md shows version 0.9.0 at the top | Read the file header |
| 79 | "Current Version" line says 0.9.0 | Check first few lines |
| 80 | v0.9.0 entry documents all 3 layers (signal scoring, Stripe, progressive access) | Read the entry |

---

## Summary

**Total requirements: 80**

The auditor should report:
- Total PASS count
- Any FAIL items with explanation
- Any edge cases or concerns not covered above
